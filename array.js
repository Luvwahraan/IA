
function getAssocValues(assoc) {
  var array = [];
  for ( var element in assoc ) {
    push(array, element);
  }
  return array;
}

function getAssocKeys(assoc) {
  var array = [];
  for ( var key : var element in assoc ) {
    push(array, key);
  }
  return array;
}
